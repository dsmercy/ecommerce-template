# API — browser logs are forwarded through the .NET API, not a separate receiver
VITE_API_BASE_URL=http://localhost:56438

# App environment label (shows up in Grafana log streams)
VITE_APP_ENV=development
